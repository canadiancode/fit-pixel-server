# Server
PORT=3001
NODE_ENV=development

# Comma-separated origins, or * for local/dev (Expo-friendly)
CORS_ORIGINS=*

# FatSecret Platform API (optional — food routes stub until set)
# https://platform.fatsecret.com/api/
FATSECRET_CLIENT_ID=
FATSECRET_CLIENT_SECRET=

# --- Future: Supabase / Postgres (not required to run this pass) ---
DATABASE_URL=
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
